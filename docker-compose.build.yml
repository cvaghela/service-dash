services:
  service-dash:
    image: service-dash:local
    build:
      context: .

  network-info:
    image: service-dash-network-info:local
    build:
      context: .
      dockerfile: Dockerfile.network-info

  kuma-auth:
    image: service-dash-kuma-auth:local
    build:
      context: .
      dockerfile: Dockerfile.kuma-auth
